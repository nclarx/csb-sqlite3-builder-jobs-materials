const faker = require('faker') // you need faker for the mockMaterial function

const helpers = require('../helpers/helpers') // remember there's the random number in range function
// createMaterials function which inserts a materials entity into the database

function createMaterial(db, jobId) {
    
    // check how the `createJob()` function is structured in `jobs`

}

function createMockMaterial() {

}



